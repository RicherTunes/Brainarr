name: Packaging Gates

on:
  schedule:
    - cron: '30 4 * * *'  # 04:30 UTC daily — staggered across ecosystem (Brainarr=base)
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: packaging-gates-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  packaging-gates:
    uses: RicherTunes/Lidarr.Plugin.Common/.github/workflows/packaging-gates.yml@1fe2da1cfb6397361a665449f39db72887a72334
    with:
      common-path: ext/Lidarr.Plugin.Common
      plugin-csproj: Brainarr.Plugin/Brainarr.Plugin.csproj
      manifest-path: plugin.json
      host-assemblies-path: ext/Lidarr-docker/_output/net8.0
      lidarr-docker-version: pr-plugins-3.1.2.4913
