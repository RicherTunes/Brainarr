# Security Hardening Backlog — brainarr

_Generated: 2026-05-23 | Phase 1 security audit_

| ID | Dimension | Finding | Severity | File:Line | Recommendation | Tracking-issue-ref |
|----|-----------|---------|----------|-----------|---------------|-------------------|
| BRN-001 | API key handling | **RESOLVED (2026-05-23).** All 8 LLM provider API keys are now stored encrypted-at-rest using Common's `IStringProtector` (`lpc:ps:v1:` format). Property setters encrypt via `EncryptApiKeyField`, getters decrypt via `DecryptApiKeyField`, and legacy plaintext values are detected with a deprecation warning. See `docs/SECURITY/BRN-001-REMEDIATION.md` for full implementation details. | High | `Brainarr.Plugin/BrainarrSettings.Providers.cs:263-373` | **COMPLETED** — Encryption implemented via `BrainarrApiKeyProtection`. See BRN-001-REMEDIATION.md for migration script (`scripts/Migrate-BrainarrSettings.ps1`). | BRN-001 |
| BRN-002 | Sensitive logging | `CorrelationContext.cs:338` (`SanitizeApiUrl`) performs a string-replace of `api_key=` → `api_key=***`, `token=` → `token=***`, `key=` → `key=***`. This is only applied inside URL sanitization, not in the general-purpose `_logger.Log*` calls scattered throughout `BrainarrOrchestrator.cs` and provider services. An exception message containing a raw API key will propagate unredacted. | High | `Brainarr.Plugin/Services/CorrelationContext.cs:330-344` | Route all log output through `Sanitize.SafeErrorMessage` (Common) before emission; or add a structured-logging enricher that redacts any property named `ApiKey`, `Token`, or `Secret`. | BRN-002 |
| BRN-003 | Sync-over-async | `BrainarrOrchestrator.FetchRecommendations` (the sync `IImportList.Fetch` implementation) delegates to `SafeAsyncHelper.RunSafeSync(() => FetchRecommendationsAsync(...))` at line 382, not a direct `.GetAwaiter().GetResult()`. `SafeAsyncHelper` uses `Task.Factory.StartNew` with `LongRunning` and `TaskScheduler.Default` to force a ThreadPool thread before blocking, mitigating deadlock risk. On ASP.NET SynchronizationContext this avoids deadlock; on ThreadPool context it works but starves the pool under high concurrency. | Medium | `Brainarr.Plugin/Services/Core/BrainarrOrchestrator.cs:374-384` | Current mitigation (dedicated thread) is documented as Category A sync-over-async debt. Long-term: expose an async IImportList variant once Lidarr supports it. | BRN-003 |
| BRN-004 | Sync-over-async | `SafeAsyncHelper.cs` contains multiple `.GetAwaiter().GetResult()` calls at lines 45, 74, 127, and 132. These are a deliberate sync-bridge wrapper that first schedules work on a dedicated thread via `StartDedicatedTask` (using `Task.Factory.StartNew` with `LongRunning` and `TaskScheduler.Default`), guaranteeing a ThreadPool context before blocking. | Medium | `Brainarr.Plugin/Utils/SafeAsyncHelper.cs:31-46` | Correct: the dedicated thread (`StartDedicatedTask`) is already in place before all `.GetAwaiter().GetResult()` calls. Documented as intentional Category A sync-over-async debt. | BRN-004 |
| BRN-005 | Path & file validation | Brainarr does not produce download file paths (it is a recommendation/import-list plugin). The `ObservabilityService.GetObservabilityHtml` method accepts a `query` parameter from external callers and does use HTML-encoding via `System.Net.WebUtility.HtmlEncode` at lines 136 and 144 before embedding in output. Stored-XSS is mitigated. | Low | `Brainarr.Plugin/Services/Core/ObservabilityService.cs:105-146` | **MITIGATED** — `WebUtility.HtmlEncode` is applied to all user-provided values before HTML embedding. | BRN-005 |
| BRN-006 | API key handling | `AIProviderFactory.cs:206` stores `_originalManualModel = settings.ManualModelId`, not an API key. The `RegistryApplicationScope` class (which contains this field) is a short-lived `IDisposable` created per provider operation and immediately disposed; it does not persist keys for the lifetime of Lidarr. API keys are retrieved fresh from encrypted settings on each request via `GetProviderApiKey`. | Low | `Brainarr.Plugin/Services/Core/AIProviderFactory.cs:186-210` | **NO ACTION NEEDED** — No long-lived key caching exists. The `_originalApiKey` claim was incorrect; the actual field is `_originalManualModel` (a model ID, not a credential). | BRN-006 |
| BRN-007 | Auth & token storage | Brainarr has no OAuth token flow (it uses API keys only). However, it accesses MusicBrainz with a custom `SecureHttpClientFactory` that explicitly sets `enableCertificatePinning: false`. Certificate pinning is disabled globally for all outbound requests with no option to enable it. | Low | `Brainarr.Plugin/Services/Http/SecureHttpClientFactory.cs:11` | Document the explicit decision not to pin; add an environment-variable opt-in (`BRAINARR_ENABLE_CERT_PINNING=true`) for hardened deployments. | BRN-007 |
| BRN-008 | Retry abuse / DoS | `MusicBrainzResolver.cs` performs MusicBrainz API lookups using `RateLimiter` (initialized at line 38) with `RateLimiterConfiguration.ConfigureDefaults` called at line 39. Rate limiting is implemented per-resolver instance. | Low | `Brainarr.Plugin/Services/Enrichment/MusicBrainzResolver.cs:34-41` | **MITIGATED** — Rate limiting is in place via `_rateLimiter` field and `ConfigureDefaults` call. User-Agent header is set via `SecureHttpClientFactory.ConfigureMusicBrainz`. | BRN-008 |
| BRN-009 | Plugin-host trust boundary | `BrainarrHealthDiagnostics.cs:36` defines `public const string ApiKey = "apiKey"` — the diagnostic endpoint key. If the diagnostics endpoint is reachable from the LAN or a misconfigured reverse proxy exposes it, it could expose settings data including API keys. | Low | `Brainarr.Plugin/Diagnostics/BrainarrHealthDiagnostics.cs:36` | Ensure the diagnostics endpoint is gated behind Lidarr's internal API key check and is never rendered with raw settings values; scrub any `ApiKey` property before serialising diagnostic output. | BRN-009 |
| BRN-010 | Sensitive logging | **CLAIM VERIFIED AS INCORRECT.** `ProviderInvoker.cs` does not log LLM prompts at `Debug` level. The `InvokeAsync` method accepts a `prompt` parameter but only passes it to the provider; there is no `_logger.Log*` call for the prompt content in `ProviderInvoker.cs`. | Low | `Brainarr.Plugin/Services/Core/ProviderInvoker.cs:14-58` | **NO ACTION NEEDED** — Prompt logging does not exist in `ProviderInvoker`. If prompt visibility is a concern, investigate provider implementations or `RecommendationGenerator` instead. | BRN-010 |
