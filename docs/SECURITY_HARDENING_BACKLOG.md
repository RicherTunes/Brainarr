# Security Hardening Backlog — brainarr

_Generated: 2026-05-23 | Phase 1 security audit_

| ID | Dimension | Finding | Severity | File:Line | Recommendation | Tracking-issue-ref |
|----|-----------|---------|----------|-----------|---------------|-------------------|
| BRN-001 | API key handling | **High-priority.** All 8 LLM provider API keys (`AnthropicApiKey`, `OpenAIApiKey`, `GeminiApiKey`, etc.) are stored as plaintext strings in Lidarr's settings JSON. The TODO comment in source (lines 88–97) explicitly acknowledges this and references `TokenProtectorFactory`. Keys are only masked in the UI via `FieldType.Password`; at-rest in Lidarr's database and config JSON they are plaintext. | High | `Brainarr.Plugin/BrainarrSettings.Providers.cs:88-97` | Implement the documented TODO: wrap each API key via `TokenProtectorFactory.CreateFromEnvironment()` at read/write time. Store an encrypted blob instead of the raw key. See `docs/API_KEY_STORAGE.md` referenced in source. | BRN-001 |
| BRN-002 | Sensitive logging | `CorrelationContext.cs:336` performs a string-replace of `api_key=` → `api_key=***`, but this is only applied inside correlation context serialization, not in the general-purpose `_logger.Log*` calls scattered throughout `BrainarrOrchestrator.cs` and provider services. An exception message containing a raw API key will propagate unredacted. | High | `Brainarr.Plugin/Services/CorrelationContext.cs:336` | Route all log output through `Sanitize.SafeErrorMessage` (Common) before emission; or add a structured-logging enricher that redacts any property named `ApiKey`, `Token`, or `Secret`. | BRN-002 |
| BRN-003 | Sync-over-async | `BrainarrOrchestrator.cs:406` calls `FetchRecommendationsAsync().GetAwaiter().GetResult()` directly — this is inside `IImportList.Fetch()` which Lidarr calls synchronously. On ASP.NET SynchronizationContext the deadlock risk is real; on ThreadPool context it works but starves the pool under high concurrency. | Medium | `Brainarr.Plugin/Services/Core/BrainarrOrchestrator.cs:406` | Wrap in `Task.Run(() => FetchRecommendationsAsync(...)).GetAwaiter().GetResult()` as a minimum fix (forces ThreadPool context). Long-term: expose an async IImportList variant once Lidarr supports it. Document as Category A sync-over-async debt. | BRN-003 |
| BRN-004 | Sync-over-async | `SafeAsyncHelper.cs` contains multiple `.GetAwaiter().GetResult()` calls (lines 45, 74, 127, 132) and a `.Result` access (line 94/127). These are a deliberate sync-bridge wrapper, but the wrapper itself does not guarantee a non-captured synchronisation context for all entry paths. | Medium | `Brainarr.Plugin/Utils/SafeAsyncHelper.cs:45,74,127,132` | Audit each call site; ensure `SafeAsyncHelper` always invokes via `Task.Run` (thread-pool hop) before `.GetAwaiter().GetResult()`. Add a comment at each entry point. | BRN-004 |
| BRN-005 | Path & file validation | Brainarr does not produce download file paths (it is a recommendation/import-list plugin), but the `ObservabilityService.GetObservabilityHtml` path at `BrainarrOrchestrator.cs:401` accepts a `query` parameter from external callers and embeds it in HTML output without HTML-encoding. Potential stored-XSS if the UI renders this in a web context. | Medium | `Brainarr.Plugin/Services/Core/BrainarrOrchestrator.cs:401` | Wrap `query` through `Sanitize.DisplayText()` (Common `Security/Sanitize.cs:66`) before including in HTML responses. | BRN-005 |
| BRN-006 | API key handling | `AIProviderFactory.cs:206` stores `_originalApiKey = GetProviderApiKey(provider, settings)` as a field on the factory. If the factory is long-lived and scoped to the singleton lifetime, the key remains in managed heap memory for the lifetime of Lidarr, increasing the window for a memory-dump attack. | Low | `Brainarr.Plugin/Services/Core/AIProviderFactory.cs:206` | Do not cache the raw key in a field; instead retrieve it fresh from settings on each request. If caching is needed, store via `SecureCredentialManager` (Common `Security/SecureCredentialManager.cs`). | BRN-006 |
| BRN-007 | Auth & token storage | Brainarr has no OAuth token flow (it uses API keys only). However, it accesses MusicBrainz with a custom `SecureHttpClientFactory` that explicitly sets `enableCertificatePinning: false`. Certificate pinning is disabled globally for all outbound requests with no option to enable it. | Low | `Brainarr.Plugin/Services/Http/SecureHttpClientFactory.cs:11` | Document the explicit decision not to pin; add an environment-variable opt-in (`BRAINARR_ENABLE_CERT_PINNING=true`) for hardened deployments. | BRN-007 |
| BRN-008 | Retry abuse / DoS | `MusicBrainzResolver.cs` (via `ArtistMbidResolver.cs`) performs MusicBrainz API lookups with no observable rate-limiting or circuit-breaker beyond what Common provides. If a Lidarr batch job triggers many concurrent import list fetches, MusicBrainz (which enforces 1 req/s for non-commercial use) may rate-limit or ban the IP. | Medium | `Brainarr.Plugin/Services/Enrichment/MusicBrainzResolver.cs` | Integrate Common's `UniversalAdaptiveRateLimiter` for the `MusicBrainz` service key; default to 1 req/s. Add appropriate `User-Agent` header per MusicBrainz guidelines (already partially addressed in `SecureHttpClientFactory`). | BRN-008 |
| BRN-009 | Plugin-host trust boundary | `BrainarrHealthDiagnostics.cs:36` defines `public const string ApiKey = "apiKey"` — the diagnostic endpoint key. If the diagnostics endpoint is reachable from the LAN or a misconfigured reverse proxy exposes it, it could expose settings data including API keys. | Low | `Brainarr.Plugin/Diagnostics/BrainarrHealthDiagnostics.cs:36` | Ensure the diagnostics endpoint is gated behind Lidarr's internal API key check and is never rendered with raw settings values; scrub any `ApiKey` property before serialising diagnostic output. | BRN-009 |
| BRN-010 | Sensitive logging | `ProviderInvoker.cs` (call chain from `RecommendationPipeline`) logs full LLM prompts at `Debug` level. Prompts can contain library metadata that some users consider private. There is no environment-level knob to disable prompt logging. | Low | `Brainarr.Plugin/Services/Core/ProviderInvoker.cs` | Add a `BRAINARR_LOG_PROMPTS=false` environment guard; by default, truncate logged prompts to 200 chars and redact any PII-like tokens. | BRN-010 |
